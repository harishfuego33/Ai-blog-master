<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Title</title>
    <style>
        /* Apply background colors */
        body {
            background-color: rgb(88, 83, 83); /* Light blue-gray */
            padding-top: 20px; /* Add some top padding */
        }

        .row {
            background-color: #ffffff; /* White */
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); /* Add shadow for depth */
        }

        .card {
            background-color: #ffffff; /* White */
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.1); /* Add shadow for depth */
        }

        /* Align categories properly */
        .categories-list {
            list-style-type: none; /* Remove bullet points */
            padding: 0;
            margin: 0;
        }

        .categories-list li {
            margin-bottom: 5px; /* Add some space between categories */
        }
    </style>
</head>
<body>
    <div class="row">
        <div class="col-md-8">
            {{#each posts}}
            <div class="card mb-4 mt-4">
                <img class="card-img-top" width="150" height="300" src="{{file}}" alt="post-img">
                <div class="card-body">
                    <h2 class="card-title">{{title}}</h2>
                    <a href="/post/{{_id}}" class="btn btn-primary">Read More &rarr;</a>
                </div>
                <div class="card-footer text-muted">
                    Posted on {{creationDate}} by <a href="#">Admin</a>
                </div>
            </div>
            {{/each}}
            <ul class="pagination justify-content-center mb-4">
                <li class="page-item">
                    <a class="page-link" href="#">&larr; Older</a>
                </li>
                <li class="page-item disabled">
                    <a class="page-link" href="#">Newer &rarr;</a>
                </li>
            </ul>
        </div>
        <div class="col-md-4">
            <div class="card my-4">
                <h5 class="card-header">Categories</h5>
                <div class="card-body">
                    <ul class="categories-list">
                        {{#each categories}}
                        <li><a href="/topic?title={{title}}">{{title}}</a></li>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
